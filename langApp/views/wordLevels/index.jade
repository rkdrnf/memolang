extends ../layout

block content
	ul
	each word in words
		li
			form(role='form', action='/wordLevels/delete', method='post')
				input(type='hidden', name='_id', value=word._id)
				span= word.owner
				br
				span= word.word
				br
				span= word.level
				br
				span= word.triedNumber
				br
				span= word.matchedNumber
				button.btn.btn-default(type='submit') Delete
					
					